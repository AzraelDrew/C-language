#include <stdio.h>
int main(){
    // int x;
    // int ret = 0;
    // x =64;
    // int t =x;
    // while(x>1){
    //     x/=2;  //每次循环后x的值回改变
    //     ret ++;
    // }
    // printf("log2 0f %d is %d.\n",t,ret);    //输出的x不是最原始的值而是计算后不满足循环的值

    int count = 100;
    while(count>0){
        printf("%d\n",count);
        count--;

    }
    printf("发射!\n");
} 